{
  "version": "5.4.2",
  "releaseDate": "2026-07-04",
  "releaseNotes": "## 新增：Kimi Code 工具\n\n接入 **Kimi Code**（月之暗面 Moonshot AI 的终端 coding agent）。应用管理中可检测、切换模型（写入 `~/.kimi-code/config.toml`）、一键装机。仅 OpenAI 协议；Anthropic 因 kimi 的 anthropic provider 路径处理不一致（404）未暴露，需要时可手动在 config.toml 配置。\n\n## 修复\n\n- **macOS 12.2 及更早**：修复在 Monterey 12.0–12.2（Safari ≤15.3）上打开任意 Markdown 页面崩溃的问题（`Object.hasOwn` polyfill）。\n- **Kimi Code 启动**：修复 Windows 上从应用管理启动 Kimi Code（curl 安装）时终端闪退的问题。\n\n## 优化\n\n窗口隐藏（最小化/切到其他应用）时，停止 LocalServer / MotherAgent / Sidebar 的后台状态轮询，减少闲置 IPC 开销。",
  "releaseNotesI18n": {
    "zh-cn": "## 新增：Kimi Code 工具\n\n接入 **Kimi Code**（月之暗面 Moonshot AI 的终端 coding agent）。应用管理中可检测、切换模型（写入 `~/.kimi-code/config.toml`）、一键装机。仅 OpenAI 协议；Anthropic 因 kimi 的 anthropic provider 路径处理不一致（404）未暴露，需要时可手动在 config.toml 配置。\n\n## 修复\n\n- **macOS 12.2 及更早**：修复在 Monterey 12.0–12.2（Safari ≤15.3）上打开任意 Markdown 页面崩溃的问题（`Object.hasOwn` polyfill）。\n- **Kimi Code 启动**：修复 Windows 上从应用管理启动 Kimi Code（curl 安装）时终端闪退的问题。\n\n## 优化\n\n窗口隐藏（最小化/切到其他应用）时，停止 LocalServer / MotherAgent / Sidebar 的后台状态轮询，减少闲置 IPC 开销。",
    "en": "## New: Kimi Code tool\n\nAdded **Kimi Code** (Moonshot AI's terminal coding agent). Detect, switch models (writes `~/.kimi-code/config.toml`), and one-click install from App Manager. OpenAI protocol only — Anthropic isn't exposed (kimi's anthropic provider path handling is inconsistent, 404s); configure it manually in config.toml if needed.\n\n## Fixes\n\n- **macOS 12.2 and earlier**: fixed a crash opening any Markdown page on Monterey 12.0–12.2 (Safari ≤15.3) — `Object.hasOwn` polyfill.\n- **Kimi Code launch**: fixed the terminal flashing closed when launching Kimi Code (curl install) from App Manager on Windows.\n\n## Improvements\n\nWhen the window is hidden (minimized / switched away), background status polling for LocalServer / MotherAgent / Sidebar is paused, reducing idle IPC overhead.",
    "zh-tw": "## 新增：Kimi Code 工具\n\n接入 **Kimi Code**（月之暗面 Moonshot AI 的終端 coding agent）。應用管理中可偵測、切換模型（寫入 `~/.kimi-code/config.toml`）、一鍵安裝。僅 OpenAI 協定；Anthropic 因 kimi 的 anthropic provider 路徑處理不一致（404）未暴露，需要時可手動在 config.toml 設定。\n\n## 修復\n\n- **macOS 12.2 及更早**：修復在 Monterey 12.0–12.2（Safari ≤15.3）上開啟任意 Markdown 頁面崩潰的問題（`Object.hasOwn` polyfill）。\n- **Kimi Code 啟動**：修復 Windows 上從應用管理啟動 Kimi Code（curl 安裝）時終端機閃退的問題。\n\n## 改善\n\n視窗隱藏（最小化 / 切換到其他應用）時，停止 LocalServer / MotherAgent / Sidebar 的背景狀態輪詢，減少閒置 IPC 開銷。",
    "ja": "## 新機能：Kimi Code ツール\n\n**Kimi Code**（Moonshot AI のターミナル coding agent）を追加。アプリマネージャーから検出・モデル切替（`~/.kimi-code/config.toml` に書き込み）・ワンクリックインストールが可能です。OpenAI プロトコルのみ——Anthropic は kimi の anthropic provider のパス処理が一貫せず（404）非公開です。必要なら config.toml で手動設定してください。\n\n## 修正\n\n- **macOS 12.2 以前**：Monterey 12.0–12.2（Safari ≤15.3）で Markdown ページを開くとクラッシュする問題を修正（`Object.hasOwn` ポリフィル）。\n- **Kimi Code 起動**：Windows でアプリマネージャーから Kimi Code（curl インストール）を起動した際、ターミナルが即座に閉じる問題を修正。\n\n## 改善\n\nウィンドウ非表示（最小化 / 別アプリへ切替）時に LocalServer / MotherAgent / Sidebar のバックグラウンド状態ポーリングを停止し、アイドル時の IPC オーバーヘッドを削減。"
  }
}
